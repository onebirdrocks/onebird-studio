import { FC } from 'react'
import { ChatStream } from '../components/ChatStream'

const Chat: FC = () => {
  return (
    <div className="flex-1">
      <ChatStream />
    </div>
  )
}

export default Chat 